# Empty = browser calls /v1 on the same host. hr-web ingress already proxies /v1.
# Do not point the SPA at hr-platform: many clients can open hr-web but not that host.
REACT_APP_API_URL=
REACT_APP_HH_API_URL=
REACT_APP_AI_API_URL=
